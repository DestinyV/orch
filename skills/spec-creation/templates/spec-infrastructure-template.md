# 基础设施分析

## 1. 服务上下游依赖

### 1.1 上游调用方
| 调用方 | 调用方式 | 接口端点 | 备注 |
|--------|---------|---------|------|
| [上游服务A] | HTTP/gRPC/MQ | POST /api/xxx | [说明调用场景] |
| [前端应用] | HTTP | GET /api/xxx | [说明调用场景] |

### 1.2 下游依赖服务
| 依赖服务 | 依赖方式 | 用途 | 容错策略 |
|---------|---------|------|---------|
| [下游服务A] | HTTP/RPC | [用途说明] | 超时/重试/降级 |
| [Redis] | 客户端 | [用途说明] | 降级方案 |

### 1.3 接口契约变更
| 接口 | 变更类型 | 变更内容 | 影响范围 |
|------|---------|---------|---------|
| POST /api/xxx | 新增/修改 | 新增字段xxx | 调用方xxx |

---

## 2. 数据库基础设施

> 数据模型和表结构设计见 `data-models.md`（逻辑模型），可执行 DDL 脚本见 `sql-ddl.md`。
> 本章节仅定义数据库运维和基础设施层面的配置。

### 2.1 数据库选型
- 数据库类型：[MySQL / PostgreSQL / SQLite / 其他]
- 版本：[版本号]
- 部署方式：[自建 / 云托管(RDS/CloudSQL) / 嵌入式]

### 2.2 连接池配置
| 配置项 | 默认值 | 说明 |
|--------|-------|------|
| 最大连接数 | [20] | 根据预估QPS和响应时间调整 |
| 最小空闲连接 | [5] | 保持可用连接 |
| 连接超时 | [30000ms] | 获取连接的超时时间 |
| 空闲回收 | [600000ms] | 空闲连接回收时间 |

### 2.3 数据迁移工具
- 迁移工具：[Flyway / Liquibase / Alembic / Prisma Migrate / 自定义]
- 迁移脚本位置：[项目内的迁移目录]
- 迁移策略：增量迁移（不可变历史，可回滚）

### 2.4 事务和一致性
- **事务范围**：[哪些操作需要事务]
- **一致性要求**：强一致性 / 最终一致性
- **并发控制**：乐观锁 / 悲观锁 / 无

### 2.5 数据保留和清理
- **保留周期**：[数据保留时间]
- **清理策略**：[定时任务清理 / TTL / 归档]
- **历史数据查询**：[是否支持，如何查询]

---

## 3. 中间件和基础设施

### 3.1 Redis
| 用途 | Key格式 | Value类型 | TTL | 淘汰策略 |
|------|---------|----------|-----|---------|
| [缓存xxx] | `app:module:{id}` | Hash | 3600s | LRU |
| [分布式锁] | `lock:module:{id}` | String | 30s | - |

### 3.2 Nacos
| 配置项 | Key | 默认值 | 说明 |
|--------|-----|--------|------|
| [配置名] | `module.xxx.enabled` | `true` | [用途说明] |

### 3.3 Docker/K8s
- **Dockerfile变更**：[新增依赖/环境变量/端口]
- **Deployment变更**：[副本数/资源限制/环境变量]
- **ConfigMap/Secret变更**：[新增配置项]
- **Service变更**：[新增/修改Service端口]

---

## 4. 项目配置

### 4.1 环境变量
| 变量名 | 环境 | 必需 | 默认值 | 说明 |
|--------|------|------|--------|------|
| XXX_API_URL | dev/test/prod | Y | - | [说明] |

### 4.2 新增依赖
| 依赖 | 版本 | 用途 | 替代方案评估 |
|------|------|------|-------------|
| [包名] | ^x.y.z | [用途] | [是否评估过替代品] |

### 4.3 构建配置变更
- [是否需要修改webpack/vite/tsconfig等]
- [变更内容和原因]

---

## 5. 标准日志规范

### 5.1 日志框架
- 日志框架：[如 winston / log4j / zap]
- 日志格式：[JSON / 文本]
- 日志级别策略：DEBUG/INFO/WARN/ERROR

### 5.2 关键业务日志
| 日志点 | 级别 | 日志内容 | 上下文字段 |
|--------|------|---------|-----------|
| [操作开始] | INFO | [描述] | userId, requestId |
| [操作成功] | INFO | [描述] | userId, requestId, result |
| [操作失败] | ERROR | [描述] | userId, requestId, error, stack |

### 5.3 链路追踪
- 是否需要 traceId：[是/否]
- traceId 生成和传递方式：[HTTP header / MQ header]
- 关键链路节点：[入口 → 服务A → 服务B → DB]

### 5.4 错误日志标准
错误日志必须包含：
- 时间戳
- 请求ID（traceId）
- 用户ID（如适用）
- 错误堆栈
- 关键业务上下文
