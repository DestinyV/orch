# 依赖注入 (DI) 指南

> code-execute 阶段代码实现参考

## 注入方式对比

| 方式 | 适用场景 | 优点 | 缺点 |
|------|---------|------|------|
| **构造函数注入** | 必须依赖 | 明确、不可变、易测试 | 参数可能过多 |
| **属性注入** | 可选依赖 | 灵活 | 依赖不明确 |
| **方法注入** | 方法级依赖 | 精确控制 | 调用时每次传递 |

## 原则

- 必须依赖用构造函数注入
- 可选依赖用属性注入
- 接口依赖倒置：依赖抽象而非具体实现
- 容器管理生命周期：单例/请求级/瞬态
